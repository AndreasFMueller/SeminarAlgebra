\documentclass[tikz]{standalone}
\usepackage{amsmath}
\usepackage{times}
\usepackage{txfonts}
\usepackage{pgfplots}
\usepackage{csvsimple}
\usetikzlibrary{arrows,intersections,math,calc}
\include{styles}
\begin{document}
\def\skala{1}
\begin{tikzpicture}[>=latex,thick,scale=\skala]
    \begin{scope}[xshift=-3.7cm]
        \fill[jcAussen] (-3.4,-3.7) rectangle (3.4,3.4);
        \fill[jcInnen, smooth, samples=300, domain=0:360, variable=\x]
        plot ({(2.2+0.4*sin(3*\x))*cos(\x)}, {(2.2+0.4*sin(3*\x))*sin(\x)}) -- cycle;
        \draw[kurvedick, smooth, samples=300, domain=0:360, variable=\x]
        plot ({(2.2+0.4*sin(3*\x))*cos(\x)}, {(2.2+0.4*sin(3*\x))*sin(\x)}) -- cycle;
        \foreach \t in {0,20,...,340}{
            \pgfmathsetmacro{\rr}{2.2+0.4*sin(3*\t)}
            \pgfmathsetmacro{\rs}{1.2*cos(3*\t)}
            \pgfmathsetmacro{\tx}{\rs*cos(\t)-\rr*sin(\t)}
            \pgfmathsetmacro{\ty}{\rs*sin(\t)+\rr*cos(\t)}
            \pgfmathsetmacro{\lng}{sqrt(\tx*\tx+\ty*\ty)}
            \pgfmathsetmacro{\px}{\rr*cos(\t)}
            \pgfmathsetmacro{\py}{\rr*sin(\t)}
            \draw[tangente]
            ({\px},{\py}) -- ({\px+0.8*\tx/\lng},{\py+0.8*\ty/\lng});
            \node[punkt, inner sep=1pt] at ({\px},{\py}) {};
        }
        \node[gebietslabel] at (0,0) {I};
        \node[gebietslabel] at (2.9,2.9) {A};
    \end{scope}
    \begin{scope}[xshift=3.7cm]
        \fill[jcAussen] (-3.4,-3.7) rectangle (3.4,3.4);
        \fill[jcInnen, smooth, samples=300, domain=0:360, variable=\x]
        plot ({(2.2+0.4*sin(3*\x))*cos(\x)}, {(2.2+0.4*sin(3*\x))*sin(\x)}) -- cycle;
        \draw[kurvedick, smooth, samples=300, domain=0:360, variable=\x]
        plot ({(2.2+0.4*sin(3*\x))*cos(\x)}, {(2.2+0.4*sin(3*\x))*sin(\x)}) -- cycle;
        \draw[dashedhelp] (0,0) circle (1.3);
        \foreach \t in {0,20,...,340}{
            \pgfmathsetmacro{\rr}{2.2+0.4*sin(3*\t)}\pgfmathsetmacro{\rs}{1.2*cos(3*\t)}
            \pgfmathsetmacro{\tx}{\rs*cos(\t)-\rr*sin(\t)}
            \pgfmathsetmacro{\ty}{\rs*sin(\t)+\rr*cos(\t)}
            \pgfmathsetmacro{\lng}{sqrt(\tx*\tx+\ty*\ty)}
            \draw[tangente] (0,0) -- ({1.3*\tx/\lng},{1.3*\ty/\lng});
        }
        \node[punktrot] (z) at (0,0) {};
        \node[font=\small, text=jcAkzent, below left=0pt of z] {$z_0$};
        \draw[jcKurve, line width=1pt, -{Stealth[length=2.5mm]}]
        (25:1.62) arc (25:335:1.62);
        \node[gebietslabel] at (0,-1.62) {I};
        \node[gebietslabel] at (2.9,2.9) {A};
    \end{scope}
\end{tikzpicture}
\end{document}

